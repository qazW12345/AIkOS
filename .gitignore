# Build artifacts (filled in as the project grows)
build/
*.o
*.elf
*.bin
*.iso
*.img
*.disk

# Old/redundant files staged for periodic review (see AGENTS.md housekeeping)
archive/

# Editor / OS junk
Thumbs.db
.DS_Store
