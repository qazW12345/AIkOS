# Build artifacts (filled in as the project grows)
build/
*.o
*.elf
*.bin
*.iso
*.img
*.disk

# Editor / OS junk
Thumbs.db
.DS_Store

# CI workflow: locally ready, push-blocked until PAT gains workflow scope (see Guides/How-to-version-control.md)
.github/workflows/
